import { cn } from "@/lib/utils";

const Spinner = () => {
  
};

export const LoadingSpinner = ({ size = 24, className = "" }: { size?: string | number | undefined, className?: string | null }) => {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      scale={size}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={cn("animate-spin", className)}
    >
      <path d="M21 12a9 9 0 1 1-6.219-8.56" />
    </svg>
  );
};